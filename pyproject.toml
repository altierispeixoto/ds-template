[tool.poetry]
name = "ds-template"
version = "0.1.0"
description = ""
authors = ["altieris.peixoto <altieris.peixoto@olist.com>"]

packages = [
    {include = "pyscaffoldext", from = "./src/"}
]

[tool.poetry.dependencies]
python = "^3.8"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

